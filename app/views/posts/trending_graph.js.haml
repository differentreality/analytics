var kind = "#{@kind}";
var category = "#{@category}";
console.log('kind: ' + kind);
alert(kind);
$('#total_reactions_' + kind + '_chart_' + category).replaceWith("#{escape_javascript(render partial: 'home/graph').html_safe}</div>");
